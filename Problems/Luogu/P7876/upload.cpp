#include <bits/stdc++.h>
using namespace std;
#define int long long
const int N = 100;
int a[N + 5][N + 5];
signed main() {
	int cas, T;
    cin >> cas >> T;
    while (T --) {
        int n, m;
        cin >> n >> m;
        for (int i = 1; i <= n; i ++)
            for (int j = 1; j <= n; j ++)
                cin >> a[i][j];
        
    }
    return 0;
}