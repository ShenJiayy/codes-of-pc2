#include <bits/stdc++.h>
using namespace std;
#define int long long
const int N = 8000;
char s[N + 5];
int dp[N + 5][N + 5];
signed main() {
    #ifndef ONLINE_JUDGE
        freopen("game.in", "r", stdin);
        freopen("game.out", "w", stdout);
    #endif
	int n;
    cin >> n;
    for (int i = 1; i <= n; i ++)
        cin >> s[i];
    // s, l
    // accabccb
    cout << 5;
    return 0;
}
