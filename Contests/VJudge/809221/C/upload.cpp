#include <bits/stdc++.h>
using namespace std;
#define int long long
const int N = 1e9, K = 6;
int p[K + 5];
signed main() {
	int n, k;
	cin >> n >> k;
	for (int i = 1; i <= k; i ++)
		cin >> p[i];
	
	return 0;
}
