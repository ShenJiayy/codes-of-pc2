老师可能考P1959 黄 遗址AC code:#include<bits/stdc++.h>  #define max(a,b) a>b?a:b  using namespace std;  bool f[5020][5020];   int x[3020];  int y[3020];  int n,m;  int main()  {  //	freopen("ruin.in","r",stdin);  //	freopen("ruin.out","w",stdout);  	scanf("%d",&n);  	for(int i=1;i<=n;i++)  	{		scanf("%d%d",x+i,y+i);		f[x[i]][y[i]]=1;	}	int ans=0;	for(int i=1;i<=n;i++)	{		for(int j=1;j<=n;j++)		{						int a=x[i]-x[j];			int b=y[i]-y[j];			int Ax=x[i]-b;			int Bx=x[j]-b;			int Ay=y[i]+a;			int By=y[j]+a;	            if(Ax<1||Ax>5000||Bx<1||Bx>5000||Ay<1||Ay>5000||By<1||By>5000)continue;			if(f[Ax][Ay]&&f[Bx][By])			{				ans=max(ans,a*a+b*b);			}		}	}	printf("%d\n",ans);    return 0;}P6116 青 家庭菜园AC code:#include<bits/stdc++.h>using namespace std;const int N=405;int n,id;int pos[N][3],sum[N][3],c0,c1,c2;int f[2][N][N][3];char s[N];int main(){	scanf("%d",&n);	scanf("%s",s+1);	for(int i=1;i<=n;i++) {		if(s[i]=='R') pos[++c0][0]=i;//第 c0 个 0 的位置		if(s[i]=='G') pos[++c1][1]=i;		if(s[i]=='Y') pos[++c2][2]=i;		sum[i][0]=sum[i-1][0]+(s[i]=='R');//前 i 位 0 的数量		sum[i][1]=sum[i-1][1]+(s[i]=='G');		sum[i][2]=sum[i-1][2]+(s[i]=='Y');	}	memset(f,0x7f,sizeof(f));	f[1][1][0][0]=pos[1][0]-1;	f[1][0][1][1]=pos[1][1]-1;	f[1][0][0][2]=pos[1][2]-1;	for(int i=2;i<=n;i++) {		id=(i&1)?1:0;		for(int j=0;j<=c0;j++) {			for(int k=0;k<=c1;k++) {				if(i-j-k>c2) continue;				if(j>0) {					int p=pos[j][0];					int w=p+max(0,k-sum[p][1])+max(0,i-j-k-sum[p][2]);					f[id][j][k][0]=min(f[id^1][j-1][k][1],f[id^1][j-1][k][2])+w-i;				}				if(k>0) {					int p=pos[k][1];					int w=p+max(0,j-sum[p][0])+max(0,i-j-k-sum[p][2]);					f[id][j][k][1]=min(f[id^1][j][k-1][0],f[id^1][j][k-1][2])+w-i;				}				if(i-j-k>0) {					int p=pos[i-j-k][2];					int w=p+max(0,j-sum[p][0])+max(0,k-sum[p][1]);					f[id][j][k][2]=min(f[id^1][j][k][0],f[id^1][j][k][1])+w-i;				}			}		}		memset(f[id^1],0x7f,sizeof(f[id^1]));	}	if(n&1) {		int ans=min(min(f[1][c0][c1][0],f[1][c0][c1][1]),f[1][c0][c1][2]);		printf("%d",(ans<1e9)?ans:-1);	}	else {		int ans=min(min(f[0][c0][c1][0],f[0][c0][c1][1]),f[0][c0][c1][2]);		printf("%d",(ans<1e9)?ans:-1);	}	return 0;}P10205 黄 室温AC code:#include<bits/stdc++.h>using namespace std;int n,a[500002],t,maxd,k;//变量名如题意int main(){    scanf("%d%d",&n,&t);	for(int i=1;i<=n;i++)	{		scanf("%d",&a[i]);		a[i]%=t;//边输入边取模	}	sort(a+1,a+n+1);	for(int i=1;i<=n;i++)		maxd=max(maxd,a[i+1]-a[i]);	maxd=max(maxd,a[1]+t-a[n]);//如题目分析，这里千万不能漏	k=ceil((double(t-maxd))/2.0);//向上取整的函数，如果它本来就是整数就不变	printf("%d",k);	return 0;}P11201 黄 数字AC code:#include<bits/stdc++.h>using namespace std;#define int long longint a[150005],b[150005],n,ans;signed main(){	ios::sync_with_stdio(false);	cin.tie(0);cout.tie(0);	cin>>n;	for(int i=0;i<n;i++)cin>>a[i];	for(int i=0;i<n;i++)cin>>b[i];	sort(b,b+n);	for(int i=0;i<n;i++){		int lim=1;//lim为10的k-1次方		for(int k=1;k<=10;k++){			if(lim*10<a[i]){//小优化，如果a[i]>10^k，那就不用算				lim*=10;				continue;			}			int ps1=lower_bound(b,b+n,lim-a[i])-b;			int ps2=lower_bound(b,b+n,lim*10-a[i])-b;			ans+=k*(ps2-ps1);//计算 k*s[i][k]			lim*=10;			if(b[n-1]+a[i]<lim)break;//和上面差不多的小优化		}	}	cout<<ans<<endl;	return 0;}P8162一堆部分分 就不放代码了P11793 黄 橙子装箱AC code：#include <bits/stdc++.h>using namespace std;int main() {    ios::sync_with_stdio(false);    cin.tie(0);        int n, m, k;    cin >> n >> m >> k;    vector<int> A(n + 1);    for (int i = 1; i <= n; i++) {        cin >> A[i];    }        vector<long long> dp(n + 1, LLONG_MAX);    dp[0] = 0;        for (int i = 1; i <= n; i++) {        int min_val = A[i];        int max_val = A[i];        for (int j = i - 1; j >= max(0, i - m); j--) {            if (A[j + 1] < min_val) min_val = A[j + 1];            if (A[j + 1] > max_val) max_val = A[j + 1];            long long cost = k + 1LL * (i - j) * (max_val - min_val);            if (dp[j] != LLONG_MAX) {                if (dp[j] + cost < dp[i]) {                    dp[i] = dp[j] + cost;                }            }        }    }        cout << dp[n] << endl;    return 0;}P11974 青 发电站AC code:#include<bits/stdc++.h>using namespace std;const int N = 2e5+7;int n;vector<int> g[N];bool imp[N];//是否有发电机int dp[N];int ans;void dfs(int x, int fx){    for(int i = 0; i < g[x].size(); i++){        int y = g[x][i];        if(y == fx) continue;        dfs(y, x);        if(imp[x]) ans = max(ans, dp[y]+1);//统计入答案        dp[x] += dp[y];    }    if(imp[x]) dp[x] = max(dp[x]-1, 1);    ans = max(ans, dp[x]);}int main(){    ios::sync_with_stdio(0);    cin.tie(0);    cin >> n;    for(int i = 1; i < n; i++){        int x, y;        cin >> x >> y;        g[x].push_back(y);        g[y].push_back(x);    }    for(int i = 1; i <= n; i++){        char c;        cin >> c;        imp[i] = c-'0';    }    dfs(1, 0);    cout << ans;    return 0;}P14296 青 高速公路#include <bits/stdc++.h>constexpr int N = 4005;long long f[N][N * 2],g[N][N * 2],F[N],G[N];// 路径可以考虑拆贡献计算 struct edge{	int to,l,c;}e[N << 2];int n,m,k,head[N],nxt[N << 2],tot = 0;inline void add(int u,int v,int l,int c){	e[++ tot].to = v;	e[tot].l = l;	e[tot].c = c;	nxt[tot] = head[u];	head[u] = tot;}inline void chkmin(long long & x,long long y){	x = (y < x ? y : x);}int main(){	std::ios::sync_with_stdio(false);	std::cin.tie(nullptr);	std::cin >> n >> m >> k;	// 设 f[i][j] 表示从 1 ~ i 经过了 j 条边的最小代价	// g[i][j] 表示从 n ~ i 经过了 j 条边的最小代价	// 对于 \sum L * k 考虑贡献计算即可	for(int i = 1; i <= n; ++ i){		for(int j = 0; j <= m; ++ j) f[i][j] = g[i][j] = 1e18;	}  	f[1][0] = 0;	g[n][0] = 0;	for(int i = 1; i <= m; ++ i){		int u,v,l,c;		std::cin >> u >> v >> l >> c;		add(u,v,l,c);	}	for(int j = 0; j < m; ++ j){		for(int i = 1; i <= n; ++ i){			for(int x = head[i]; x ; x = nxt[x]){				int v = e[x].to,l = e[x].l,c = e[x].c;				chkmin(f[v][j + 1],f[i][j] + c + 1ll * (j + 1) * k * l);				chkmin(g[i][j + 1],g[v][j] + c + 1ll * j * k * l);			}		}	}	long long ans = 1e18;	for(int i = 1; i <= n; ++ i){		F[i] = G[i] = 1e18;		for(int j = 0; j <= m; ++ j){			chkmin(F[i],f[i][j]);			chkmin(G[i],g[i][j]);		}//		std::cout << F[i] << ' ' << G[i] << '\n';		chkmin(ans,F[i] + G[i]);	}	if(ans == 1e18) std::cout << -1 << '\n';	else std::cout << ans << '\n';	return 0;}P12457 黄 台球AC code：#include<bits/stdc++.h>#define int long longusing namespace std;const int nm=2e5+5;int n,m,x,k;int a[nm],vis[nm],p[nm],son[nm],val[nm],flag;int dfs(int i){	if(vis[i]==1) return x+1;	if(p[i]==-1) return a[i];	if(val[i]) return val[i];	vis[i]=1;	int ans=dfs(p[i]);	vis[i]=0;	return val[i]=a[i]+ans;}signed main(){	ios::sync_with_stdio(0);	cin.tie(0);	cout.tie(0);	cin>>n>>x;	for(int i=1;i<=n;i++) cin>>a[i];	for(int i=1;i<=n;i++) cin>>p[i];	for(int i=n;i>=1;i--){		if(dfs(i)<=x){			cout<<i;			flag=1;			break;		}	}	if(!flag) cout<<-1;	return 0;}P11794 黄 集邮首先，要放 J 就放开头，要放 I 就放末尾就会最优。 这可由子序列的定义直接推出。对于 O 而言，如果它放下标为 i?1 和 i 的字母之间，其贡献是 i?1 及以前的 J 的数量乘 i 及以后的 I 的数量。其中 1≤i≤n+1。 因为这个 O 加进来后，原来的数量不变，直接加上其贡献即可。所以只需给 J 的数量做前缀和，I 的数量做后缀和即可。P14328 黄 糖AC mind: 题目大意 给定 n 个糖果，第 i 个糖果价值为 A i?，连续 K 个糖果中至多选 2 个，求最大价值和。思路分析 此题 N 和 K 较少，考虑使用 DP。定义状态 dp i,j?表示最后一个和倒数第二个选中的糖果分别是第 i 个和第 j 个时，能获得的最大收益。此时可以发现 dp i,j?(j<i) 可以从 dp j,k?(k<min(j?1,max(0,i?k))) 转移过来，可以三层循环枚举 i,j,k 暴力转移。接下来考虑边界，容易得到 dp i,0?=A i?。复杂度 O(n^3 )，无法通过本题，就不贴代码了我永远不会告诉你我根本就没写这个代码。优化 1 定义 pre i,j?为 dp i,0?到 dp i,j?中的最大值，可以得到两个转移方程：dp i,j?=pre j,min(j?1,max(i?m,0))?+A i?pre i,j?=max(pre i,j?1?,dp i,j?) 边界：dp i,0?=A i?，pre i,0?=A i?。P14331 绿 煎饼题意简述称一个字符串串为“烤串”当且仅当这个串 AA...ABB...BCC...C 的形式（即 A 全部在最前面， C 全部在最后面， B 全部在中间）。 给定一个由 A、B、C 三种字符组成的字符串，你需要通过最少次数的操作将这个串变成“烤串”。 操作：将原串从 1 到 n 的位置反转。 输出最少的次数。 思路首先看范围 2≤N≤13 时限 2.5s ，那么 3 N 的复杂度是可接受的。 可以将原串变为烤串的过程逆推（发现给定操作的逆操作依然是这个操作）。 从最终的烤串出发，最多有 3 N 种状态，可以 BFS 预处理所有状态最少的操作步数。 O(1) 查询。P10206 绿 建设在不新建铁路线的图上跑最短路，定义此时 x,y 间的最短路长为 dis(x,y)。特判掉 dis(S,T)≤K 的情况（所有方案均合法），此时我们要求的就是满足 dis(S,u)+L+dis(T,v)≤K 的无序二元组 (u,v) 数量。令 a i?=dis(T,i)，将 a 数组从小到大排序。枚举 u，则满足条件的 v 数量有 p 个，其中 p 为最大的满足 dis(S,u)≤K?L?a p?的数（不存在则为 0），直接 upper_bound 即可。答案为 ∑p，时间复杂度 O(mlogm+nlogn)。P11663 绿 打怪#include<bits/stdc++.h>using namespace std;//不开long long见祖宗 #define ll long longconst ll MAXN=5e5+10,MAXLL=4611686018427387903;ll n,a[2*MAXN],b[2*MAXN],sum[2*MAXN],w[2*MAXN],q[2*MAXN],ans=MAXLL;int main() {	scanf("%lld",&n);	for(int i=1; i<=n; i++) {		scanf("%lld",&a[i]);		a[i+n]=a[i];	}	for(int i=1; i<=n; i++) {		scanf("%lld",&b[i]);		b[i+n]=b[i];	}	//前缀和 	//计算sum[]、w[] 	for(int i=1; i<=2*n-1; i++) {		sum[i]=sum[i-1]+b[i];		w[i]=a[i]-sum[i-1];	}	//使用单调队列 	int head=1,tail=0;	for(int i=1; i<=n-1; i++) {		while(w[q[tail]]<=w[i]&&head<=tail) tail--;		q[++tail]=i;	}	for(int i=n; i<=2*n-1; i++) {		while(q[head]<i-n+1&&head<=tail) head++;		while(w[q[tail]]<=w[i]&&head<=tail) tail--;		q[++tail]=i;		ans=min(ans,w[q[head]]+sum[i-n]);	}	printf("%lld",ans);	return 0;}P14332 [JOI2021 预选赛 R2] 活动巡游 / Event Hopping 动态规划 DP 二分#include<bits/stdc++.h>#define int long longusing namespace std;int n,d,k,dp[200001],mx1,mx2;pair<int,int> p[200001];vector<pair<int,int> > r1,r2;signed main(){	ios::sync_with_stdio(0);cin.tie(0);	cin>>n>>d>>k;	for(int i=1;i<=n;i++)cin>>p[i].second>>p[i].first;	sort(p+1,p+n+1);	r1.push_back({0,0}),r2.push_back({0,0});	for(int i=1;i<=n;i++){		if(p[i].second==1){			int l=0,r=r2.size()-1,ans=0;			while(l<=r){				int mid=l+r>>1;				if(r2[mid].first<p[i].first)ans=r2[mid].second,l=mid+1;				else r=mid-1;			}			dp[i]=max(mx1,ans)+1;			r1.push_back({p[i].first+d+k*dp[i],dp[i]});			mx1=dp[i];		}else{			int l=0,r=r1.size()-1,ans=0;			while(l<=r){				int mid=l+r>>1;				if(r1[mid].first<p[i].first)ans=r1[mid].second,l=mid+1;				else r=mid-1;			}			dp[i]=max(mx2,ans)+1;			r2.push_back({p[i].first+d+k*dp[i],dp[i]});			mx2=dp[i];		}	}	cout<<max(mx1,mx2);}P15809 [JOI 2013 Final] JOIOI 塔 / Tower of JOIOI 贪心 二分二分答案，考虑如何检查 x 是否合法。 如果要制作 x 个塔，必须要 x 个 I 作为塔底，这些 I 需要尽量往后，以作为更多塔的塔底。 选出塔底后，需要 x 个 O 为塔身，需要满足逆序遍历到任何时候，O 的数量不超过塔底 I 的数量。 最后是作为塔顶的 I 或 J，同理数量需要不超过塔身 O 的数量。#include <bits/stdc++.h>using namespace std;#define int long longint n;string s;bool check(int x){	int tp = 0, md = 0, bt = 0;	for(int i = n; i; -- i){		if(s[i] == 'J')			if(tp < md)				++ tp;		if(s[i] == 'O')			if(md < bt)				++ md;		if(s[i] == 'I'){			if(bt < x)				++ bt;			else if(bt == x && tp < md)				++ tp;		}	}	return tp >= x;}signed main(){	ios::sync_with_stdio(0);cin.tie(0);cout.tie(0);	cin >> n >> s;	s = " " + s;	int l = 0, r = n / 3 + 1, ans = 0;	while(l <= r){		int mid = (l + r) >> 1;		if(check(mid))			l = mid + 1, ans = mid;		else			r = mid - 1;	}	cout << ans;	return 0;}P15823 [JOI Open 2013] 观影 / Watching 动态规划DP 二分#include <bits/stdc++.h>using namespace std;int n,p,q;int a[2005];int f[2005][2005];bool check(int x){	memset(f,0x3f,sizeof f);	for(int i = 1;i <= p;i++){		f[n][i] = 0;	} // 记得初始化	for(int k = n-1;k >= 1;k--){		int l,r;		for(int i = k;i <= n;i++){			if(a[k]+x-1 >= a[i]) l = i; // 求第一个无法覆盖的点			else break;		}		for(int i = k;i <= n;i++){			if(a[k]+2*x-1 >= a[i]) r = i;			else break;		}		for(int i = 0;i <= p;i++){			if(i-1 >= 0){  // 需要注意一下边界				if(l == n) f[k][i] = 0;				else f[k][i] = min(f[k][i],f[l+1][i-1]); // 转移方程			}			if(r == n) f[k][i] = min(f[k][i],1);			else f[k][i] = min(f[k][i],f[r+1][i]+1);		}	}	for(int i = 0;i <= p;i++){		if(f[1][i] <= q) return 1;	}	return 0;}int main(){	scanf("%d %d %d",&n,&p,&q);	p = min(p,n);	for(int i = 1;i <= n;i++){		scanf("%d",&a[i]);	}	sort(a+1,a+n+1);	int l = 1,r = 1e9,mid,ans;	while(l <= r){ // 二分答案		mid = (l+r)/2;		if(check(mid)){			r = mid-1;			ans = mid;		}		else l = mid+1;	}	cout << ans;	return 0;}P11789 [JOI 2019 Final] 画展 / Exhibition 贪心 排序 双指针 two-pointer第一眼肯定是贪心。 显然可以先把所有的画以美观值和大小为第一，第二关键字从小到大排序，因为为了先保证美观满足条件，再对画框大小的限制进行贪心。 然后从后往前枚举，先匹配大的画框,再用剩下的匹配小的画框。容易证明大的画框也一定能装下小的画，肯定不会更劣。 然后你就能完成这道题啦！#include <bits/stdc++.h>using namespace std;const int N = 1e6 + 5;int n,m,ans,b[N];pair<int,int> a[N];int main(){    ios::sync_with_stdio(false);    cin.tie(0);    cin >> n >> m;    for (int i = 0; i < n; i++)        cin >> a[i].second >> a[i].first;//倒着输入，因为美观是第一关键字，大小是第二关键字    for (int i = 0; i < m; i++)        cin >> b[i];    sort(a, a + n);//利用pair自带的排序    sort(b, b + m);    for (int i = n - 1, j = m - 1; i >= 0 && j >= 0; i--)//从后往前    {        if (b[j] >= a[i].second)//如果匹配的上就更新答案，并和下一个匹配        {            ans++;            j--;        }    }    cout << ans;//输出答案    return 0;闲话：作者真不想活了